# frozen_string_literal: true

module SolarJuice
  module PartnerApi
    # Tracks the OpenAPI document's info.version. The conformance test asserts
    # the two stay in step, so a spec bump cannot ship without a gem bump.
    VERSION = "1.0.0"
  end
end
