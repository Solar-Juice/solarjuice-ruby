# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Test and build tooling only. The gem itself has no runtime dependencies, and
# nothing here is needed to use it.
group :development do
  gem "minitest", "~> 5.18"
  gem "rake", "~> 13.0"
end
